# /api/destinations 를 대신한다.
#
# 지금 응답은 waypoint 이름 배열뿐이라, 웹 UI 는 그 내부 코드를 그대로 버튼에
# 찍고 voice_node 는 그 코드가 발화에 들어 있기만 기대한다. 표시 이름·종류·층이
# 함께 와야 버튼도 층 전환도 성립한다.
#
# 요청은 비어 있다. 안내 노드는 waypoint 파일 하나(= 한 층)를 들고 있고,
# 지금 걸러 낼 축이 없다. 층 여러 개를 동시에 들게 되면 그때 필드를 더한다.
---
guide_interfaces/Poi[] pois

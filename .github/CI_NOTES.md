# CI 노트

> 파일명이 README.md면 GitHub가 이 폴더를 저장소 메인 페이지 README로 채택해버립니다
> (.github/ → 루트 → docs/ 순으로 README를 찾음). 그래서 일부러 다른 이름으로 둡니다.

CI 파이프라인(GitHub Actions workflows)은 여기 추가 예정.
현재는 workflow 파일이 없어 비어 있음 — 실제 워크플로 추가 시
Personal Access Token에 `workflow` scope가 필요합니다.
